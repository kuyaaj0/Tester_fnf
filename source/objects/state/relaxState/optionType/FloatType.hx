package objects.state.relaxState.optionType;

class FloatType extends FlxSpriteGroup
{
    //defeat的值填ClientPrefs里的对应值
    public function new(X:Int = 0, Y:Int = 0, lable:String = 'test', defeat:Float, max:Int, min:Int, bit:Int){
    
    }
}