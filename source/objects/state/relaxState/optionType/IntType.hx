package objects.state.relaxState.optionType;

class IntType extends FlxSpriteGroup
{
    //defeat的值填ClientPrefs里的对应值
    public function new(X:Int = 0, Y:Int = 0, label:String = 'test', defeat:Int, max:Int, min:Int){
    
    }
}